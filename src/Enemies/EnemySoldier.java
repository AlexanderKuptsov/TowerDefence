package Enemies;

import Graphics.TileGrid;

public class EnemySoldier extends Enemy {

    public EnemySoldier(EnemyType enemyType, int tileX, int tileY, TileGrid grid) {
        super(enemyType, tileX, tileY, grid);
    }
}
